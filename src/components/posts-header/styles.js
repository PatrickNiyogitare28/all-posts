import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        display: 'flex',
        height: 30,
        justifyContent:'space-between',
        flexDirection: 'row',
        marginTop: 20
    },
    avatar: {
        width: 50,
        height: 50,
        borderRadius: 50
    }
})

export {styles}